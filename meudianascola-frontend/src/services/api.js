import axios from 'axios';export const api = axios.create({baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api', headers: { 'Content-Type': 'application/json' }});export async function verifyFace(faceIds){const token=localStorage.getItem('meudianascola_token');const res=await api.post('/face/verify',{faceIds},{headers:{Authorization:`Bearer ${token}`}});return res.data;}