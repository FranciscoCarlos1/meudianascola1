import { useEffect, useState } from 'react';import PhotoUpload from '../../components/PhotoUpload';import { api } from '../../services/api';export default function TeacherDashboard(){const [students,setStudents]=useState([]);useEffect(()=>{const token=localStorage.getItem('meudianascola_token');api.get('/students',{headers:{Authorization:`Bearer ${token}`}}).then(res=>setStudents(res.data)).catch(err=>console.error(err));},[]);return (<div className="p-6"><h1 className="text-2xl font-bold mb-4">Painel do Professor</h1><div className="grid grid-cols-3 gap-4">{students.map(s=> (<div key={s.id} className="border p-4 rounded shadow-sm"><h2 className="font-semibold">{s.name}</h2><p>Turma: {s.classId}</p><PhotoUpload studentId={s.id} /></div>))}</div></div>);}